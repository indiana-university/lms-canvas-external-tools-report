
projectedSecret:
  - secret:
      name: "properties-2021-06-07"
  - secret:
      name: "lms-security-properties-20201014"

image:
  tag: stable

env:
  APP_ENV: prd
  
activeProfiles: "redis,prd"

serviceAccountName: external-tools-report-prd

cluster: es-prod

ingress:
  enabled: true
  hosts:
    - host: "external-tools-report.apps.iu.edu"
      paths: []