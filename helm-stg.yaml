
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

activeProfiles: "redis,stg"

serviceAccountName: external-tools-report-stg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "external-tools-report-stg.apps-test.iu.edu"
      paths: []