
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

activeProfiles: "redis,reg"

serviceAccountName: external-tools-report-reg

cluster: es-test

ingress:
  enabled: true
  hosts:
    - host: "external-tools-report-reg.apps-test.iu.edu"
      paths: []
